/// <reference types="jest" />
// import {create} from '../src/app';

describe('test', () => {
  it('equals', () => {
    expect(1).toEqual(1);
  });
});

// describe('create', () => {
//   it('is method', () => {
//     expect(typeof create).toEqual('function');
//   });
// });
